require(tictoc)
require(RJSONIO) #to read json files
require(openxlsx) #To open excel files
require(stringr) #To strip the direction from the names
require(fastmatch) # faster match function
require(lubridate) #To extract the month/day etc ...
require(fasttime)
require(chron) #is.weekend function
require(knitr) #kable tables
require(ggplot2)
require(ggthemes) #some themes for better looking plots
require(data.table)
require(tidyverse)